# Record optional dependencies for every platform in package-lock.json, not
# just the one that ran `npm install`. Without this, a lockfile generated on
# Windows omits Linux-only transitive deps (e.g. @emnapi/* via sharp) and
# `npm ci` fails on the Ubuntu CI runner with "Missing: ... from lock file".
os=linux,darwin,win32
cpu=x64,arm64
